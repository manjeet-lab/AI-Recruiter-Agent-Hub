# Core UI framework
streamlit>=1.35.0

# Google AI SDK and ADK
# google-adk 2.x requires the Kludex starlette fork (starlette 1.x).
# Pin starlette to avoid the corrupt cached wheel bug.
google-genai>=0.1.1
google-adk>=2.0.0
starlette==1.3.1

# Environment and configuration
python-dotenv>=1.0.1

# PDF parsing
pypdf>=4.2.0

# Async compatibility (bridges Streamlit's event loop with asyncio.run)
nest_asyncio>=1.6.0

# Data validation
pydantic>=2.7.0

# Model Context Protocol (MCP) — Filesystem Server + Client
mcp>=1.0.0

# Enables loading environment variables from a .env file.
python-dotenv
